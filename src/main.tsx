import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import ReturnPeriodSimulator from "./ReturnPeriodSimulator";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ReturnPeriodSimulator />
  </React.StrictMode>
);
